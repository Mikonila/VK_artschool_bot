# VK Bot с ИИ

Простой VK-бот с интеграцией OpenAI GPT для ответов на сообщения пользователей.

## Возможности

- 🤖 Автоматические ответы с использованием OpenAI GPT-3.5-turbo
- 💬 Поддержка команд (/start, /help, /status)
- 📝 Логирование всех операций
- ⚙️ Гибкая конфигурация через переменные окружения
- 🛡️ Обработка ошибок

## Установка

1. **Клонируйте репозиторий или скачайте файлы**

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте конфигурацию (выберите один способ):**

   **Способ 1 - Интерактивная настройка:**
   ```bash
   python setup_env.py
   ```
   
   **Способ 2 - Ручное создание файла `.env`:**
   ```env
   # VK Bot Configuration
   VK_TOKEN=your_vk_bot_token_here
   VK_GROUP_ID=your_group_id_here

   # OpenAI Configuration
   OPENAI_API_KEY=your_openai_api_key_here

   # Bot Settings
   BOT_NAME=AI Assistant
   MAX_MESSAGE_LENGTH=4096
   ```

4. **Проверьте настройки:**
   ```bash
   python test_tokens.py
   ```

## Настройка

### 1. Создание VK-бота (обновлено для нового интерфейса VK)

1. Перейдите на [dev.vk.com/ru/admin/apps-list](https://dev.vk.com/ru/admin/apps-list)
2. Создайте новое **"Мини-приложение"**
3. Создайте группу VK и включите "Long Poll API"
4. Получите токен через OAuth (см. NEW_VK_GUIDE.md)
5. Скопируйте токен и ID группы в файл `.env`

**📖 Подробная инструкция:** [NEW_VK_GUIDE.md](NEW_VK_GUIDE.md)

### 2. Получение OpenAI API ключа

1. Зарегистрируйтесь на [platform.openai.com](https://platform.openai.com)
2. Создайте API ключ в разделе API Keys
3. Добавьте ключ в файл `.env`

## Запуск

```bash
python bot.py
```

## Команды бота

- `/start`, `/help`, `помощь`, `команды` - показать справку
- `/status`, `статус` - проверить статус бота
- Любое другое сообщение - получить ответ от ИИ

## Структура проекта

```
vk_bot/
├── bot.py                 # Основной код бота
├── config.py              # Конфигурация
├── requirements.txt       # Зависимости
├── setup_env.py          # Интерактивная настройка .env
├── test_tokens.py        # Проверка токенов
├── VK_SETUP_GUIDE.md     # Подробная инструкция по VK
├── env_example.txt       # Пример файла .env
├── .env                  # Переменные окружения (создать самостоятельно)
└── README.md             # Документация
```

## Логирование

Бот ведет подробные логи всех операций. Логи выводятся в консоль в формате:
```
2024-01-01 12:00:00 - bot - INFO - Бот AI Assistant успешно инициализирован
```

## Обработка ошибок

- Автоматическая обработка ошибок API
- Логирование всех исключений
- Graceful shutdown при Ctrl+C

## Требования

- Python 3.7+
- VK API токен
- OpenAI API ключ
- Интернет соединение

## Лицензия

MIT License
